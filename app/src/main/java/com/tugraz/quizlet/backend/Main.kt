package com.tugraz.quizlet.backend

class Main {
    fun main() {
        // TODO: Add database object
        /*val dbInterface : DBInterface = null
        val requestHandler = RequestHandler(dbInterface)
        val server = WebServer(requestHandler)
        server.startServer()*/
    }
}